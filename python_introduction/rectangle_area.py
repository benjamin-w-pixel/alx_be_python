# rectangle_area.py

# Define two variables: length and width, with specified values.
length = 10
width = 5

# Calculate the area of the rectangle using the formula Area = length * width
# Store the result in a variable named area.
area = length * width

# Print the calculated area in the format: The area of the rectangle is: [area].
print(f"The area of the rectangle is: {area}")